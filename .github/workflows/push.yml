name: test

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

  docker-publish:
    uses: ./.github/workflows/docker-publish.yml
    with:
      version: v0.2.0
    secrets: inherit
